apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pwa-infra.fullname" . }}-test-connection"
  labels:
    {{- include "pwa-infra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pwa-infra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
